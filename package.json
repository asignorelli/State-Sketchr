{
  "name": "state-sketcher-hackathon",
  "version": "1.0.0",
  "private": true,
  "description": "Deployment configuration for State Sketcher",
  "scripts": {
    "build": "mkdir -p public && esbuild index.tsx --bundle --outfile=public/main.js --loader:.tsx=tsx --jsx=automatic && cp index.html public/index.html"
  },
  "dependencies": {
    "@google/genai": "^1.20.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.3.3"
  }
}